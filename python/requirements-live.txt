# Compiled with --no-deps on purpose, so this list is the complete runtime set
# rather than a resolver's closure. Two consequences worth knowing before editing:
#
# 1. cronometer-mcp is no longer here. Its client is vendored at
#    python/vendor/cronometer_client.py instead: upstream stopped at 2026-03-08
#    with food search already broken by a Cronometer change and four unmerged
#    fixes waiting, and a pinned dependency cannot be patched. `requests` is now
#    the only runtime dependency this project does not own.
# 2. requests' transitive dependencies are pinned here by hand for the same reason.
#    If requests gains a new one, `uv pip sync` will not add it for you.
#
# Regenerate with:
#   uv pip compile python\requirements-live.txt -o python\requirements-live.lock \
#     --generate-hashes --no-deps --python-platform windows --python-version 3.12
requests==2.34.2
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.18
urllib3==2.7.0
